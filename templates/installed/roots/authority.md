# Greenhouse Authority

This file is human-owned.

## Ownership

- `roots/` files are authored policy.
- `grown/` files are refreshable repo knowledge.
- `scripts/` files are generated helpers.
- package scripts are ergonomic aliases, not source of truth.
