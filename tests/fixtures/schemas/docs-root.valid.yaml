schema_version: 1
tracked_docs:
  - path: README.md
    owns:
      - setup
      - package-scripts
      - validation
    covers:
      - path: src/context/**
        reason: README describes context workflow.
        strictness: warning
  - path: docs/cli.md
    owns:
      - cli
